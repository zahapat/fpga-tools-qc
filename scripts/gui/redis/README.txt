Terminal 1: (cmd)
    wsl
    sudo passwd -d <username>         (will make account passwordless)
    sudo apt install redis-server     (install redis)
    sudo apt update && apt upgrade    (update & upgrade apps)
    sudo service redis-server start   (Start Redis Server)
    sudo service redis-server stop    (Stop Redis Server)
    exit

Terminal 2: (PowerShell)
    
